version: 2
defaults: &defaults
  working_directory: ~/repo
  docker:
  - image: circleci/node:10.5.0

jobs:
  test:
    <<: *defaults
    steps:
    - checkout
    - run: npm install
    - run: npm run lint
    - run: npm test
workflows:
  version: 2
  test:
    jobs:
    - test
